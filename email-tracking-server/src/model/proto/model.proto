syntax = "proto3"

service ModelService{
    rpc CheckModel (CheckModelRequest) returns (CheckModelResponse)
}

message CheckModelRequest{
    string model=1;
    string provider = 2;
    string api_key=3;
    string api_key_type=4;
}

message CheckModelResponse{
    bool valid = 1;
    string message = 2;
}